Load the strategic planning skill from `.claude/skills/strategic-planning/SKILL.md` in this project folder.

Then execute `/sp-sync`: read the skill's version number and command definitions, then update CLAUDE.md to reflect:
1. The current active skill version in the "Skill version" section.
2. The commands table in the "Commands" section — replace it with the commands as currently defined in the skill.

Confirm to the user what was updated and what the new skill version is.
