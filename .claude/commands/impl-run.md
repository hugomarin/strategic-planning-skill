Load the implementation context skill from `.claude/skills/technical-context/SKILL.md` in this project folder.

Then execute `/impl-run` as defined in the skill, passing any artifact name provided as argument (e.g. `stack`, `schema`, `feature:editor`). Generate or regenerate that artifact. Create a new version if one already exists.
