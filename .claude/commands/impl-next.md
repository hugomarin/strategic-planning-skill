Load the implementation context skill from `.claude/skills/technical-context/SKILL.md` in this project folder.

Then execute `/impl-next` as defined in the skill: generate the next pending artifact in sequence (stack → schema → architecture → features). Present a summary and wait for confirmation before proceeding.
