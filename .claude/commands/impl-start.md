Load the implementation context skill from `.claude/skills/technical-context/SKILL.md` in this project folder.

Then execute `/impl-start` as defined in the skill: invoke the Diagnostic Analyst with scope `implementation`, compose `_technical_analysis.md`, and present the Input Analysis Summary. Wait for human confirmation before generating any artifact.
