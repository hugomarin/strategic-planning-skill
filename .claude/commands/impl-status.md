Load the implementation context skill from `.claude/skills/technical-context/SKILL.md` in this project folder.

Then execute `/impl-status` as defined in the skill: show the current state of all implementation artifacts in `Technical Artifacts/` — complete, in progress, missing, or marked NHR.
